# ─── Web framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # file upload support

# ─── Data validation ─────────────────────────────────────────────────────────
pydantic>=2.7.0

# ─── Image processing ────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0.80
numpy>=1.26.0
Pillow>=10.3.0                   # fallback decoder / thumbnail generation

# ─── ML inference backends (install the one you need) ────────────────────────
# ONNX Runtime (recommended for production)
onnxruntime>=1.18.0
# onnxruntime-gpu>=1.18.0        # GPU variant — uncomment if CUDA available

# PyTorch (optional — only needed if using .pt model)
# torch>=2.3.0
# torchvision>=0.18.0

# TFLite (optional — only needed for edge deployment)
# tflite-runtime>=2.14.0

# ─── Utilities ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.1             # .env file support
httpx>=0.27.0                    # async HTTP client (tests)

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
fastapi
uvicorn
numpy
opencv-python
matplotlib
scipy
scikit-image
pandas
python-multipart
pydantic
pillow
python-dotenv
httpx
pytest
onnxruntime
torch
torchvision
onnx
onnxruntime
albumentations
scikit-learn
tqdm
